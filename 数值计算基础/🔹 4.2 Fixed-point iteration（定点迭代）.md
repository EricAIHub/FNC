## 🌟 为什么学习不动点迭代？

当我们要解一个非线性方程：

$$
f(x)=0
$$


一种基本思路是把它改写成 **等价的固定点问题**：

> 找一个值 $p$ 使得：
> 
> $$
g(p)=p
$$

> 
> 这个 $p$ 就是 **不动点（fixed point）**。

如果能把原来的问题变成这个形式，就可以用不动点迭代去逼近这个 $p$。

---

## 🌟 什么是“不动点问题”？

👉 **Definition 4.2.1**  
给定一个函数 $g(x)$，求一个值 $p$ 使得：

$$
g(p)=p.
$$


如果我们从 $f(x)=0$ 出发，可以令：

$$
g(x)=x-f(x)
$$


那么如果 $r$ 是 $f(r)=0$ 的根，那么：

$$
g(r)=r
$$


也就是说，这样的 $r$ 也是 $g$ 的不动点。

实际上还有很多不同方式把 $f(x)=0$ 改写成 $g(x)=x$ 的形式，比如：

$$
g(x)=x+c\,f(x)
$$


只要 $g(r)=r$ 和 $f(r)=0$ 等价即可。

---

## 🌟 不动点迭代算法

最基本的迭代方法就是：

📌 **Algorithm 4.2.2 — Fixed-point iteration**

选择一个初始猜测 $x_1$，然后重复：

$$
x_{k+1}=g(x_k), \quad k=1,2,\dots
$$


这意味着：

> 不断把当前的估计值带入 $g$ 得到下一次估计。

如果序列 $x_1,x_2,\dots$ 收敛到某个值 $p$，那么这个值满足：


$$
p=g(p)
$$

也就是不动点。

---

## 🌟 图像直觉

不动点迭代的几何意义：

- 在平面上画出 $y=g(x)$ 和直线 $y=x$；
    
- 不动点就是二者的交点；
    
- 迭代过程相当于：从 $x_k$ 出发 → 到 $y=g(x_k)$ → 垂直映射回直线 $x=x_{k+1}$；
    
- 不断重复这个“水平 → 垂直”移动，期望收敛到交点。
    

---

## 🌟 收敛性分析（用误差理解）

用误差表示：

$$
\varepsilon_k=x_k-p
$$


从不动点定义有：

$$
x_{k+1}=g(x_k)
$$


Taylor 展开 $g$ 于 $p$ 附近：


$$
g(p+\varepsilon_k)=g(p)+g'(p)\varepsilon_k+O(\varepsilon_k^2)
$$


因为 $g(p)=p$，所以：

$$
\varepsilon_{k+1}\approx g'(p)\varepsilon_k+O(\varepsilon_k^2)
$$


如果误差要收敛到 0，那么主要线性项需要满足：

> **$|g'(p)| <1$** → 误差逐步缩小  
> 否则误差会发散。

---

## 🌟 什么是**线性收敛（linear convergence）**？

当误差满足：

$$
\lim_{k\to\infty}\frac{|\varepsilon_{k+1}|}{|\varepsilon_k|}=\sigma<1
$$


就称这个迭代 **线性收敛（linear convergence）**，收敛率约为 $\sigma$。  
这就是说误差每步大约缩小一个固定比例。

如果用对数看误差，可以看到误差随迭代次数的线性下降趋势，这就是“线性收敛”的具体表现形式。

---

## 🌟 不动点迭代的典型例子

假设我们要找某二次方程 $f(x)=0$ 的根，把它变成：

$$
g(x) = x - f(x)
$$


在图上看 $y=g(x)$ 和 $y=x$ 的交点：

- 有时候迭代会稳定收敛；
    
- 有时候迭代会发散。
    

这取决于 $g'(p)$ 的绝对值。

---

## 🌟 更强的收敛条件：**收缩映射（contraction map）**

如果函数 $g(x)$ 在某区间 $S$ 上满足 **Lipschitz 条件**：

$$
|g(s)-g(t)|\le L|s-t|,\quad L<1
$$


那么：

1. $g$ 在 $S$ 中有且仅有一个不动点；
    
2. 对任意起点在 $S$ 中，不动点迭代都会收敛于这个不动点；
    
3. 误差有界：
    

$$
|g(s)-g(t)|\le L|s-t|,\quad L<1
$$


这就说明了线性收敛的具体条件。

---

## 🌟 你必须记住的核心结论

| 核心点       | 要义                                                             |
| --------- | -------------------------------------------------------------- |
| **不动点定义** | $p$ 满足 $g(p)=p$                                                |
| **根查找等价** | 把 $f(x)=0$ 写成 $g(x)=x$                                         |
| **迭代公式**  | $x_{k+1}=g(x_k)$                                               |
| **收敛条件**  | 若函数 g(x)在不动点 p处的导数 ∣g′(p)∣<1，则不动点迭代法收敛到 p，并且收敛速度与 ∣g′(p)∣ 成正比。 |
| **收缩映射**  | Lipschitz 常数 $L<1$ → 全局收敛                                      |

---

## 🌟 直觉总结

- 不动点迭代是最简单的迭代方法。
    
- 它**不保证收敛**，只有当 $g$ 足够“平缓”时（$|g'|<1$）才会收敛。
    
- 如果收敛，它通常是**线性收敛**（不很快）。
    
- 它常用于 **作为更高级方法（如 Newton）的理论基础**。